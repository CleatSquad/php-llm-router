{
    "name": "mohaelmrabet/php-llm-router",
    "description": "Provider-agnostic LLM client for PHP — a uniform interface over Claude, OpenAI, Gemini, Mistral, Groq, DeepSeek, Ollama and Kimi, with pluggable routing, retries, caching, circuit breaking, rate limiting, MCP and A2A client drivers, embeddings, and audio transcription.",
    "type": "library",
    "license": "MIT",
    "keywords": ["llm", "ai", "claude", "openai", "gemini", "mistral", "groq", "deepseek", "ollama", "router", "fallback", "circuit-breaker", "rate-limit", "mcp", "model-context-protocol", "a2a", "agent2agent", "agent", "embeddings", "audio", "transcription", "whisper"],
    "require": {
        "php": ">=8.2",
        "guzzlehttp/guzzle": "^7.8",
        "mcp/sdk": "^0.7.0",
        "psr/log": "^3.0",
        "psr/simple-cache": "^3.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0",
        "phpstan/phpstan": "^2.0"
    },
    "suggest": {
        "ext-redis": "Needed for RedisCacheStore/RedisCircuitBreakerStore/RedisRateLimitStore, so cache/breaker/rate-limit state is shared across requests, worker processes and machines.",
        "ext-apcu": "Needed for ApcuRateLimitStore — the Redis-free way to share an atomic quota across the PHP-FPM workers of one machine.",
        "symfony/cache": "Any PSR-16 implementation works with Psr16CacheStore/Psr16CircuitBreakerStore when Redis isn't available (filesystem, APCu, Memcached, PDO, ...)."
    },
    "autoload": {
        "psr-4": {
            "LlmRouter\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "LlmRouter\\Tests\\": "tests/"
        },
        "files": [
            "tests/Fixtures/FakeRedis.php"
        ]
    },
    "scripts": {
        "test": "phpunit",
        "test:security": "phpunit --group security",
        "phpstan": "phpstan analyse"
    },
    "minimum-stability": "stable"
}
